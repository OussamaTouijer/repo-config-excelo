# Server
server.port=9005

# PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/excelo_notification_service-db
spring.datasource.username=postgres
spring.datasource.password=oussama
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

spring.main.allow-bean-definition-overriding=true

# Firebase Configuration (disabled for development)
firebase.enabled=false
firebase.service-account-key=
firebase.project-id=excelo-app-dev
firebase.database-url=

# Redis Configuration (if needed)
spring.redis.host=localhost
spring.redis.port=6379
spring.redis.timeout=60000

# Flyway configurations removed